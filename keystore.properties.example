# Copy to keystore.properties in the repo root (that file is gitignored).
# storeFile can be an absolute path.
storeFile=/Users/shirasawa/keys/uvc-release.jks
storePassword=
keyAlias=uvc
keyPassword=
